#---------------------------------------------
# A way to generate a secret key for the API:
#---------------------------------------------

# import secrets

# # Generate a random secret key
# secret_key = secrets.token_hex(32)  # Generate a 256-bit (32-byte) hex secret key
# print("Secret Key:", secret_key)